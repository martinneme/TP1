


/** \brief
 * GetInt()
 * \param int* Envia la posicion de memoria de la variable en main que guardara el valor ingresado en la funcion
 * \param char[] Envia a la funcion el mensaje a colocar en el printf que le solicita al usuario el valor
 * \return void No devuelve ningun valor por return (podria ser utilizado en tipo INT para retornar un indicador de exito o error)
 *
 */
void GetInt(int*,char[]);
/** \brief
 * suma()
 * \param int Envia a la funcion el valor del primer input
 * \param int Envia a la funcion el valor del segundo input
 * \param int* Envia a la funcion la posicion de memoria de la variable en main que almacenara el resultado de la operacion.
 * \return void No devuelve ningun valor por return (podria ser utilizado en tipo INT para retornar un indicador de exito o error)
 *
 */
void suma(int,int,int*);
/** \brief
 * resta()
 * \param int Envia a la funcion el valor del primer input
 * \param int Envia a la funcion el valor del segundo input
 * \param int* Envia a la funcion la posicion de memoria de la variable en main que almacenara el resultado de la operacion.
 * \return void No devuelve ningun valor por return (podria ser utilizado en tipo INT para retornar un indicador de exito o error)
 *
 */
void resta(int,int,int*);
/** \brief
multiplica()
 *\param int Envia a la funcion el valor del primer input
 *\param int Envia a la funcion el valor del segundo input
 *\param int* Envia a la funcion la posicion de memoria de la variable en main que almacenara el resultado de la operacion.
 *\return void No devuelve ningun valor por return (podria ser utilizado en tipo INT para retornar un indicador de exito o error)
 *
 */
void multiplica(int,int,int*);
/** \brief
 * divide()
 *\param int Envia a la funcion el valor del primer input
 *\param int Envia a la funcion el valor del segundo input
 *\param float* Envia a la funcion la posicion de memoria de la variable en main que almacenara el resultado de la operacion.
 *\return void No devuelve ningun valor por return (podria ser utilizado en tipo INT para retornar un indicador de exito o error)
 *
 */
void divide(int,int,float*);
/** \brief
 * factorial()
 * \param int Envia a la funcion el valor a calcular factorial.
 * \return long int Devuelve el resultado de la funcion factorial
 *
 */
long int factorial(int);
